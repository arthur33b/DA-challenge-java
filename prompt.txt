Você é um assistente especialista em desenvolvimento backend com Java, Spring Boot e boas práticas de arquitetura.  
Crie uma aplicação **Spring Boot** completa (nível acadêmico/profissional) que implemente uma **API RESTful de leitura**, para uma **Dashboard de Cursos da Alura**, seguindo os requisitos abaixo.

---

# 🎯 OBJETIVO
Construir uma **API REST** em **Java + Spring Boot + Spring Data JPA** que **consulta cursos e exibe estatísticas**, sem permitir alterações.  
O foco é exibir **os cursos mais populares por área**, com base em dados persistidos no banco de dados.

---

# 🧩 FUNCIONALIDADES PRINCIPAIS

1. ✅ Consultar todos os cursos disponíveis;
2. ✅ Consultar cursos por área (categoria);
3. ✅ Consultar os cursos mais populares (por número de alunos matriculados, avaliações ou notas médias simuladas);
4. ✅ Retornar estatísticas agregadas:
   - Quantidade de cursos por categoria;
   - Top 5 cursos mais populares;
   - Média de avaliações por área.

⚠️ **Não incluir endpoints de criação, edição ou exclusão (somente leitura).**

---

# 🧱 REQUISITOS TÉCNICOS

- Linguagem: **Java 17+**
- Framework: **Spring Boot 3+**
- Banco de dados: **Oracle** (ou **H2** para ambiente de desenvolvimento)
- Persistência: **Spring Data JPA**
- Entidades modeladas com **anotações JPA (@Entity, @Table, @OneToMany, etc.)**
- Consultas com **JPQL** ou **Spring JPA Query Methods**
- Estrutura de pacotes:
com.alura.dashboard
├── controller
├── service
├── repository
├── model (ou entity)
├── dto
└── config


# 🧠 MODELO DE DOMÍNIO

Entidades sugeridas:

### 📘 Curso
- `id` (Long)
- `nome` (String)
- `descricao` (String)
- `categoria` (Categoria)
- `instrutor` (Instrutor)
- `popularidade` (Integer) → número de matrículas simuladas
- `notaMedia` (Double) → avaliação média simulada

### 🧑‍🏫 Instrutor
- `id` (Long)
- `nome` (String)
- `email` (String)
- `especializacao` (String)
- `cursos` (List<Curso>)

### 🏷️ Categoria
- `id` (Long)
- `nome` (String)
- `descricao` (String)
- `cursos` (List<Curso>)

Relacionamentos:
- Curso → Categoria (ManyToOne)
- Curso → Instrutor (ManyToOne)
- Categoria → Cursos (OneToMany)
- Instrutor → Cursos (OneToMany)

---

# 🌐 ENDPOINTS REST (somente GET)

- `GET /api/cursos` → lista todos os cursos
- `GET /api/cursos/categoria/{id}` → lista cursos por categoria
- `GET /api/cursos/populares` → retorna cursos mais populares (ordenados por popularidade)
- `GET /api/estatisticas/por-categoria` → retorna quantidade de cursos por categoria
- `GET /api/estatisticas/top5` → retorna os 5 cursos mais populares
- `GET /api/estatisticas/avaliacoes` → média de notas por área

---

# ⚙️ CONFIGURAÇÃO
- Configurar `application.properties` para Oracle e H2.
- Usar `schema.sql` e `data.sql` para criar tabelas e dados simulados (ex: cursos com popularidade e notas fictícias).
- Implementar o padrão **Service** e **Repository** com coesão e baixo acoplamento.

---

# 🧩 DESIGN E BOAS PRÁTICAS
- Aplicar os princípios da **POO** (encapsulamento, herança, abstração e polimorfismo).
- Usar **DTOs** para saída das consultas.
- Documentar a API com **Swagger / OpenAPI**.
- Garantir coesão e baixo acoplamento entre camadas.
- Seguir o modelo de maturidade REST **nível 1 (Richardson)**.
- Implementar **Repository Genérico** se possível.

---

# 🧪 TESTES
- Criar uma **coleção Postman ou Insomnia** com todas as consultas (GET).
- Exportar o JSON da coleção para a pasta `/postman`.
- Demonstrar que os endpoints retornam corretamente os dados simulados do banco.

---

# 📚 DOCUMENTAÇÃO E ENTREGA
- Criar README.md contendo:
- Nome da aplicação
- Integrantes e papéis
- Instruções de execução (Oracle e H2)
- Imagens dos diagramas (classe e DER)
- Lista de endpoints
- Link para vídeo de apresentação
- Link do repositório GitHub público

- Incluir uma pasta `/docs` com:
- Diagrama de Classes (Entidades)
- Diagrama DER
- Cronograma da equipe

---

# 🚀 ENTREGA ESPERADA
- Projeto Java Spring Boot funcional, com consultas REST
- Banco de dados populado (dados simulados)
- Documentação e diagramas no repositório GitHub
- Swagger e Postman exportáveis
